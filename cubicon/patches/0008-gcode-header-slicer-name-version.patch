diff --git a/src/libslic3r/Config.cpp b/src/libslic3r/Config.cpp
index 89e8302904..837fa065a7 100644
--- a/src/libslic3r/Config.cpp
+++ b/src/libslic3r/Config.cpp
@@ -1360,6 +1360,13 @@ ConfigSubstitutions ConfigBase::load_from_gcode_file(const std::string &file, Fo
         std::string orcaslicer_gcode_header = std::string("; generated by ");
         orcaslicer_gcode_header += SLIC3R_APP_NAME;
 
+        // Cubicon: the G-code header emits the upstream official name (SLIC3R_OFFICIAL_NAME =
+        // "OrcaSlicer") for Moonraker/Mainsail compatibility (see header_slic3r_generated), and
+        // legacy OrcaForCubicon / OrcaSlicer files also use "OrcaSlicer" in the header. Accept that
+        // form too, otherwise the app rejects loading its own and older G-code files.
+        std::string official_gcode_header = std::string("; generated by ");
+        official_gcode_header += SLIC3R_OFFICIAL_NAME;
+
         std::string header;
         bool        header_found = false;
         while (std::getline(ifs, header)) {
@@ -1370,7 +1377,8 @@ ConfigSubstitutions ConfigBase::load_from_gcode_file(const std::string &file, Fo
             line_c = skip_whitespaces(line_c);
             // BBS
             if (strncmp(bambuslicer_gcode_header.c_str(), line_c, strlen(bambuslicer_gcode_header.c_str())) == 0 ||
-                strncmp(orcaslicer_gcode_header.c_str(), line_c, strlen(orcaslicer_gcode_header.c_str())) == 0) {
+                strncmp(orcaslicer_gcode_header.c_str(), line_c, strlen(orcaslicer_gcode_header.c_str())) == 0 ||
+                strncmp(official_gcode_header.c_str(), line_c, strlen(official_gcode_header.c_str())) == 0) {
                 header_found = true;
                 break;
             }
diff --git a/src/libslic3r/utils.cpp b/src/libslic3r/utils.cpp
index 42dcc14878..098fa5d191 100644
--- a/src/libslic3r/utils.cpp
+++ b/src/libslic3r/utils.cpp
@@ -1230,7 +1230,15 @@ std::string string_printf(const char *format, ...)
 
 std::string header_slic3r_generated()
 {
-	return std::string(SLIC3R_APP_NAME " " SoftFever_VERSION);
+	// Cubicon: the G-code header identifies the slicer to Moonraker/Mainsail, which only parses
+	// metadata (object height, layer height, filament, print time, ...) when the "generated by"
+	// line starts with a slicer name it recognizes (OrcaSlicer/PrusaSlicer/BambuStudio/...).
+	// "OrcaForCubicon" is not recognized -> shows "Unknown" and no metadata. So emit the upstream
+	// official name (SLIC3R_OFFICIAL_NAME = "OrcaSlicer") followed by the OrcaForCubicon product
+	// version (CUBI_ORCA_VERSION, e.g. "1.5.0-rc1"), NOT the upstream SoftFever_VERSION ("2.4.2").
+	// GCodeProcessor still recognizes this via its literal "generated by OrcaSlicer" producer tag,
+	// and Config.cpp's G-code loader accepts the "generated by OrcaSlicer" header (see there).
+	return std::string(SLIC3R_OFFICIAL_NAME " " CUBI_ORCA_VERSION);
 }
 
 std::string header_gcodeviewer_generated()
