diff --git a/src/CMakeLists.txt b/src/CMakeLists.txt
index 73c767dad2..210cfabf11 100644
--- a/src/CMakeLists.txt
+++ b/src/CMakeLists.txt
@@ -186,7 +186,9 @@ if (WIN32)
     endif()
     target_compile_definitions(OrcaSlicer_app_gui PRIVATE -DSLIC3R_WRAPPER_NOCONSOLE)
     add_dependencies(OrcaSlicer_app_gui OrcaSlicer)
-    set_target_properties(OrcaSlicer_app_gui PROPERTIES OUTPUT_NAME "orca-slicer")
+    # Cubicon: user-facing GUI executable is OrcaForCubicon.exe (the shim still loads OrcaSlicer.dll
+    # by its fixed name, so only the .exe is renamed).
+    set_target_properties(OrcaSlicer_app_gui PROPERTIES OUTPUT_NAME "OrcaForCubicon")
     target_link_libraries(OrcaSlicer_app_gui PRIVATE boost_headeronly)
 endif ()
 
